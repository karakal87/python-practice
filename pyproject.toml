[project]
name = "python-practice"
version = "0.1.0"
description = "A repo to practice Python codeing."
authors = [
    {name = "Thomas Reynolds",email = "71638374+karakal87@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
